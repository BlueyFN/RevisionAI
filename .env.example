# Rename to .env.local (Next.js) or .env (Node) and fill in your secrets

# Required: OpenAI API key used by the RevisionAI chatbot
OPENAI_API=

# Optional: default model identifier for chat completions
OPENAI_MODEL=gpt-4o

# Optional: public URL used by the client when constructing callbacks
NEXT_PUBLIC_APP_URL=http://localhost:3000
